# Automotive-ECU-Power-Manager
Built a synthesizable Verilog IP core that acts as a hardware-level power and startup supervisor for automotive ECUs. Instead of relying on software delay loops, it manages power rail sequencing, filters out voltage transients from engine cranking, and handles fault recovery using a deterministic state machine.
